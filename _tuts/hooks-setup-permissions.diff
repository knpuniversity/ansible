diff --git a/ansible/deploy.yml b/ansible/deploy.yml
index bffbeb2..1cac3f3 100644
--- a/ansible/deploy.yml
+++ b/ansible/deploy.yml
@@ -13,6 +13,9 @@
 
   vars:
     release_console_path: "{{ ansistrano_release_path.stdout }}/bin/console"
+    release_var_path: "{{ ansistrano_release_path.stdout }}/var"
+    release_logs_path: "{{ release_var_path }}/logs"
+    release_sessions_path: "{{ release_var_path }}/sessions"
 
     # Ansistrano vars
     ansistrano_deploy_to: "{{ project_deploy_dir }}" # Base path to deploy to.
diff --git a/ansible/deploy/after-symlink-shared.yml b/ansible/deploy/after-symlink-shared.yml
index cd2dc82..89dea4a 100644
--- a/ansible/deploy/after-symlink-shared.yml
+++ b/ansible/deploy/after-symlink-shared.yml
@@ -14,3 +14,23 @@
 
 - name: Warm up the cache
   command: '{{ release_console_path }} cache:warmup --env=prod'
+
+- name: Setup directories permissions in var/ dir
+  become: true
+  command: 'find {{ release_var_path }} -type d -exec chmod 0555 {} \;'
+
+- name: Setup files permissions in var/ dir
+  become: true
+  command: 'find {{ release_var_path }} -type f -exec chmod 0444 {} \;'
+
+- name: Setup directories permissions in annotations/ dir
+  become: true
+  command: 'find {{ release_var_path }}/cache/prod/annotations -type d -exec chmod 0777 {} \;'
+
+- name: Setup directories permissions in logs/ dir
+  become: true
+  command: 'find {{ release_logs_path }} -type d -exec chmod 0777 {} \;'
+
+- name: Setup directories permissions in sessions/ dir
+  become: true
+  command: 'find {{ release_sessions_path }} -type d -exec chmod 0777 {} \;'
diff --git a/ansible/playbook.yml b/ansible/playbook.yml
index db6aef2..421af88 100644
--- a/ansible/playbook.yml
+++ b/ansible/playbook.yml
@@ -87,6 +87,14 @@
         value: UTC
       notify: Restart PHP-FPM
 
+    # Allows Ansistrano to remove cache files that have 0664 permissions and www-data owner/group
+    - name: Add current user to the www-data group
+      become: true
+      user:
+        name: '{{ ansible_user }}'
+        groups: www-data
+        append: yes
+
     - name: Check for Composer
       stat:
         path: /usr/local/bin/composer

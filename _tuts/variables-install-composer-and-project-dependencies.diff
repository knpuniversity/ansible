diff --git a/ansible/playbook.yml b/ansible/playbook.yml
index c76b218..1561018 100644
--- a/ansible/playbook.yml
+++ b/ansible/playbook.yml
@@ -63,3 +63,10 @@
 
     - name: Checkout Git repository
       git: repo=https://github.com/symfony/symfony-standard.git dest="{{ symfony_root_dir }}" force=yes
+
+    - name: Install Composer
+      become: yes
+      get_url: url=https://getcomposer.org/composer.phar dest=/usr/local/bin/composer mode="a+x"
+
+    - name: Install Composer's dependencies
+      composer: working_dir="{{ symfony_root_dir }}"
